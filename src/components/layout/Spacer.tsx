import { StyledBasicSpacer } from "./StyledComponents";
import { SpacerProps } from "./Types";
import React from "react";

export const Spacer = (props: SpacerProps) => <StyledBasicSpacer {...props} />;
